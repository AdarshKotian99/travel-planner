import { TurncateDescriptionPipe } from './turncate-description.pipe';

describe('TurncateDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TurncateDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
